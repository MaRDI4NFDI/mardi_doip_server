# ---------------------------------------------------------------------------------------
# This is the config file for the web app - please change the values to match your setup.
# ---------------------------------------------------------------------------------------

# Set this to "True" to enable password protection - make sure to set the password in ./streamlit/secrets.toml
use_startup_password: False

# Path to ChromaDB directories
chroma_db:
  global_path: "../chroma"
  local_path: "./chroma"
  embedding_function: 'huggingface/BAAI/bge-base-en-v1.5'

# Ollama server settings
ollama:
  host: 'localhost'
  path: ''
  port: 11434
  timeout: 2
  use_ssl: False
  api_key: ''
  standard_model: 'qwen2:1.5b'
  max_context_size: 8000

# Sparql
sparql:
  source: 'mardi'

# Streamlit page settings
streamlit:
  page_title: "MaRDI PoC LLM Query Web App"
  page_icon: "💬"
  layout: "wide"

# Example queries
example_queries:
  - "Show me a dataset about Covid."
  - "Show me a formula describing epidemiology."
  - "What is the Maxwell-Stefan model?"
  - "Q6140887: Summarize the given text in one sentence"

# AI Model that should be shown first if available
ai_standard_model: "qwen2:0.5b"

# Number of answers settings
default_answers: 3
